
[ Proxy (프록시) Pattern ]

프록시 패턴
- 어떤 객체에 대한 접근을 제어하기 위한 용도로 
  대리인이나 대변인에 해당하는 객체를 제공하는 패턴

기본적인 프록시 종류
- 원격 프록시
- 가상 프록시
- 보호 프록시

프록시
- 진짜 객체를 대신하는 역할

원격 프록시
- 원격 객체에 대한 대변자 역할

원격 객체
- 다른 주소 공간에서 동작하고 있는 객체

